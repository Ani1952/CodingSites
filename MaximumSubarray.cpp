#include <bits/stdc++.h>

using namespace std;

int main()
{

    int T;
    cin >> T;
    while (T)
    {
        int size, subsize,n;
        cin >> size >> subsize;
        cout<<endl;
        int *a=new int(size);
        for (int i = 0; i < size; i++)
        {
            cin>>n;
            a[i]=n;
        }

        deque<int> d;
        for(int i=0;i<size;i++){
            
        }
        
    }

    return 0;
}